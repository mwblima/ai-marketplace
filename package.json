{
  "name": "ai-marketplace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A governance model for sharing AI artifacts (skills, agents, MCP servers, hooks) across a company.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "targets": "node scripts/build-targets.mjs",
    "validate": "node scripts/validate.mjs",
    "test": "node --test \"tests/*.test.mjs\"",
    "check": "node scripts/build.mjs --check && node scripts/build-targets.mjs --check && npm run validate && npm run test",
    "ci": "npm run build && npm run targets && npm run validate && npm run test",
    "serve": "npm run build && node scripts/serve.mjs"
  },
  "dependencies": {
    "yaml": "^2.8.1"
  },
  "engines": {
    "node": ">=20"
  }
}
